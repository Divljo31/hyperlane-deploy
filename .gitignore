.env
.env.*
*.key
*.pem
node_modules/
.DS_Store

# strategy.yaml contains private keys
configs/strategy.yaml
strategy.yaml

hyperlane_db_relayer/
generated/

# Deploy env files hold secrets (KMS creds / RPC keys); keep the .example templates committed.
# (basename pattern matches at any depth, so this also covers deploy/*/.env.example)
!.env.example

# Mainnet signer material, if ever kept locally
configs/strategy.mainnet.yaml
